# External request UNK-0018-CME: Timestamp semantics for CME market data: which field is exchange event time, which is rece

Blocker: `UNK-0018-CME` | stage `M1_BLOCKING` | method `EXTERNAL_ACTION` | tier 3
Priority score: 12.0 | generated by `M1/orchestrator/resolvers.py`

## PURPOSE

A written answer from CME market-data support, or a sample file whose sequence and timestamp fields are validated against the project's timestamp contract.

## CANDIDATES AFFECTED

`TUP-CME-ES-H1-QDEP-PAS`, `TUP-CME-ES-H3-OFI-AGG`, `TUP-CME-NQ-H3-OFI-AGG`, `TUP-CME-TSY-H2-QREPL-MIX`, `TUP-CME-WTI-H4-FLOWVOL-AGG`

## DECISION BLOCKED

Causal replay for every CME candidate (KG2).

## EXACT INFORMATION REQUIRED

Which field on MDP 3.0 messages is the exchange event time, which is receive time, what clock domain each uses, and what ordering guarantee the sequence numbers provide across the A/B feeds.

## EXACT PRODUCT / INSTRUMENT

CME MDP 3.0 MBO Full Depth and MBP for ES and NQ

## DATE RANGE

Sample: one continuous trading day, plus the field dictionary for that version

## REQUIRED SCHEMA AND FIELDS

Per-message field semantics with units and precision, and whether two messages can share a timestamp with a defined order

## REQUIRED TIMESTAMP FIELDS AND CLOCK DOMAIN

Explicitly: monotonic ordering within a channel, cross-channel (A/B) ordering, duplication handling, gap fill, and any required clock synchronisation

## LICENSING, COST AND COMMITMENT QUESTIONS

Whether the sample may be used for research and retained

## SAMPLE FILE REQUEST

One day of MBO or MBP data with the field dictionary

## ACCEPTABLE SUBSTITUTE

A written protocol answer to the ordering questions is sufficient if no sample can be released.

## WHAT AN ANSWER KILLS

Causal replay for every CME candidate (KG2).

## WHAT AN ANSWER CLEARS

A written answer from CME market-data support, or a sample file whose sequence and timestamp fields are validated against the project's timestamp contract.

## READY-TO-SEND TEXT

```
Subject: Request for CME MDP 3.0 MBO Full Depth and MBP for ES and NQ specification, fee schedule and sample data

Hello,

We are evaluating CME MDP 3.0 MBO Full Depth and MBP for ES and NQ for a systematic trading research programme. To decide whether it is economically and technically feasible we need the following, in writing:

1. Which field on MDP 3.0 messages is the exchange event time, which is receive time, what clock domain each uses, and what ordering guarantee the sequence numbers provide across the A/B feeds.
2. The applicable fee schedule for our account type, with effective dates.
3. Confirmation of the historical data product that corresponds to this instrument, including history depth, delivery format and price.
4. A sample file (one trading day is sufficient) so we can validate the schema and timestamp semantics before purchase.
5. Any entitlement, membership or minimum-commitment requirement.

Please state explicitly if any of these is not published or not available, and what the alternative would be. We would rather have an explicit 'not available' than an approximation, because our feasibility decision depends on it.

Thank you.
```

## WHERE TO RETURN THE ANSWER

Write the response into `M1/work/patches/UNK-0018-CME_response.json` using the patch schema in
`M1/orchestrator/schemas.py`, or paste the raw vendor document into
`M1/work/external_requests/UNK-0018-CME_received/` and record the patch by hand.
