# External request UNK-0018: Historical and live feed timestamp semantics

Blocker: `UNK-0018` | stage `M1_BLOCKING` | method `EXTERNAL_ACTION` | tier 3
Priority score: 30.0 | generated by `M1/orchestrator/resolvers.py`

## PURPOSE

Field-level timestamp semantics (event vs receive time, clock domain, sequence integrity)

## CANDIDATES AFFECTED

`ALL_CANDIDATES`

## DECISION BLOCKED

Causal replay and therefore every queue, priority or fill claim; applies to any candidate that reaches replay.

## EXACT INFORMATION REQUIRED

Field-level timestamp semantics for the sample files: which field is the exchange event time, which is receive time, the clock domain, and whether ordering is guaranteed by sequence number.

## EXACT PRODUCT / INSTRUMENT

Every feed entering the M2 replay: CME MBO/MBP, Nasdaq order-level, and any other candidate feed

## DATE RANGE

Sample: one trading day per feed

## REQUIRED SCHEMA AND FIELDS

Field-by-field semantics with units and precision

## REQUIRED TIMESTAMP FIELDS AND CLOCK DOMAIN

Explicit answers: monotonic ordering within a channel, cross-channel ordering, duplication, gap-fill, and clock synchronisation requirements

## LICENSING, COST AND COMMITMENT QUESTIONS

Not applicable beyond the enclosing data licence

## SAMPLE FILE REQUEST

One day of data with a field dictionary

## ACCEPTABLE SUBSTITUTE

A written protocol specification answering the ordering questions is acceptable if a sample cannot be released.

## WHAT AN ANSWER KILLS

Timestamps cannot support causal ordering, invalidating replay for that feed

## WHAT AN ANSWER CLEARS

Samples validate: monotonic ordering, sequence integrity, documented clock domain

## READY-TO-SEND TEXT

```
Subject: Request for Every feed entering the M2 replay: CME MBO/MBP, Nasdaq order-level, and any other candidate feed specification, fee schedule and sample data

Hello,

We are evaluating Every feed entering the M2 replay: CME MBO/MBP, Nasdaq order-level, and any other candidate feed for a systematic trading research programme. To decide whether it is economically and technically feasible we need the following, in writing:

1. Field-level timestamp semantics for the sample files: which field is the exchange event time, which is receive time, the clock domain, and whether ordering is guaranteed by sequence number.
2. The applicable fee schedule for our account type, with effective dates.
3. Confirmation of the historical data product that corresponds to this instrument, including history depth, delivery format and price.
4. A sample file (one trading day is sufficient) so we can validate the schema and timestamp semantics before purchase.
5. Any entitlement, membership or minimum-commitment requirement.

Please state explicitly if any of these is not published or not available, and what the alternative would be. We would rather have an explicit 'not available' than an approximation, because our feasibility decision depends on it.

Thank you.
```

## WHERE TO RETURN THE ANSWER

Write the response into `M1/work/patches/UNK-0018_response.json` using the patch schema in
`M1/orchestrator/schemas.py`, or paste the raw vendor document into
`M1/work/external_requests/UNK-0018_received/` and record the patch by hand.
