# External request UNK-0018-NASDAQ: Timestamp semantics for Nasdaq order-level history and the live TotalView feed: event vers

Blocker: `UNK-0018-NASDAQ` | stage `M1_BLOCKING` | method `EXTERNAL_ACTION` | tier 3
Priority score: 18.0 | generated by `M1/orchestrator/resolvers.py`

## PURPOSE

The ITCH message specification's timestamp section plus a sample day whose ordering and sequence fields validate against the project's contract.

## CANDIDATES AFFECTED

`TUP-NASDAQ-LARGETICK-H2-QIMB-AGG`, `TUP-NASDAQ-LARGETICK-H2H3-MICRO-AGG`, `TUP-NASDAQ-ANYCAP-H4-AUCTION-AGG`

## DECISION BLOCKED

Causal replay and therefore queue/fill claims for the Nasdaq equity rows (KG2).

## EXACT INFORMATION REQUIRED

The timestamp field semantics of the TotalView-ITCH message set: which field is exchange event time, whether receive time is included, and how sequence numbers guarantee ordering across the historical files.

## EXACT PRODUCT / INSTRUMENT

NASDAQ Historical TotalView-ITCH (and the live TotalView feed)

## DATE RANGE

Sample: one session of historical files plus the message specification's timestamp section

## REQUIRED SCHEMA AND FIELDS

Message specification with the timestamp fields marked and their units

## REQUIRED TIMESTAMP FIELDS AND CLOCK DOMAIN

Within-file ordering, ordering across the multiple files of a session, duplicate/gap handling, and clock domain

## LICENSING, COST AND COMMITMENT QUESTIONS

Whether the specification may be quoted in internal documentation

## SAMPLE FILE REQUEST

One session of ITCH files, or the specification's timestamp section

## ACCEPTABLE SUBSTITUTE

The message specification alone answers most of this; the sample confirms ordering empirically.

## WHAT AN ANSWER KILLS

Causal replay and therefore queue/fill claims for the Nasdaq equity rows (KG2).

## WHAT AN ANSWER CLEARS

The ITCH message specification's timestamp section plus a sample day whose ordering and sequence fields validate against the project's contract.

## READY-TO-SEND TEXT

```
Subject: Request for NASDAQ Historical TotalView-ITCH (and the live TotalView feed) specification, fee schedule and sample data

Hello,

We are evaluating NASDAQ Historical TotalView-ITCH (and the live TotalView feed) for a systematic trading research programme. To decide whether it is economically and technically feasible we need the following, in writing:

1. The timestamp field semantics of the TotalView-ITCH message set: which field is exchange event time, whether receive time is included, and how sequence numbers guarantee ordering across the historical files.
2. The applicable fee schedule for our account type, with effective dates.
3. Confirmation of the historical data product that corresponds to this instrument, including history depth, delivery format and price.
4. A sample file (one trading day is sufficient) so we can validate the schema and timestamp semantics before purchase.
5. Any entitlement, membership or minimum-commitment requirement.

Please state explicitly if any of these is not published or not available, and what the alternative would be. We would rather have an explicit 'not available' than an approximation, because our feasibility decision depends on it.

Thank you.
```

## WHERE TO RETURN THE ANSWER

Write the response into `M1/work/patches/UNK-0018-NASDAQ_response.json` using the patch schema in
`M1/orchestrator/schemas.py`, or paste the raw vendor document into
`M1/work/external_requests/UNK-0018-NASDAQ_received/` and record the patch by hand.
