# External request UNK-0018-USSTOCK-CROSS: Cross-venue synchronisation for the national-market tuple: can several venue feeds be plac

Blocker: `UNK-0018-USSTOCK-CROSS` | stage `M1_BLOCKING` | method `EXTERNAL_ACTION` | tier 3
Priority score: 2.0 | generated by `M1/orchestrator/resolvers.py`

## PURPOSE

Per-feed timestamp semantics plus a documented synchronisation method and its error bound.

## CANDIDATES AFFECTED

`TUP-USSTOCK-XVENUE-H1-STALEQUOTE-AGG`

## DECISION BLOCKED

Causal cross-venue comparison, and therefore the stale-quote tuple (KG2/KG3).

## EXACT INFORMATION REQUIRED

Whether several venue feeds can be placed on one comparable timeline: per-feed timestamp semantics and a documented synchronisation method with its error bound.

## EXACT PRODUCT / INSTRUMENT

Multi-venue US equity depth (Nasdaq, Cboe, NYSE, IEX)

## DATE RANGE

Current feeds; sample one day per venue

## REQUIRED SCHEMA AND FIELDS

Per-feed field semantics plus the consolidator's normalisation rules

## REQUIRED TIMESTAMP FIELDS AND CLOCK DOMAIN

Clock domains, synchronisation method, measured skew, and how the consolidator resolves ordering

## LICENSING, COST AND COMMITMENT QUESTIONS

Whether the feeds may be recorded and combined for research

## SAMPLE FILE REQUEST

One day of two or more venue feeds with their specifications

## ACCEPTABLE SUBSTITUTE

A written synchronisation statement with an error bound is sufficient for the M1 decision; the measurement itself is M2.

## WHAT AN ANSWER KILLS

Causal cross-venue comparison, and therefore the stale-quote tuple (KG2/KG3).

## WHAT AN ANSWER CLEARS

Per-feed timestamp semantics plus a documented synchronisation method and its error bound.

## READY-TO-SEND TEXT

```
Subject: Request for Multi-venue US equity depth (Nasdaq, Cboe, NYSE, IEX) specification, fee schedule and sample data

Hello,

We are evaluating Multi-venue US equity depth (Nasdaq, Cboe, NYSE, IEX) for a systematic trading research programme. To decide whether it is economically and technically feasible we need the following, in writing:

1. Whether several venue feeds can be placed on one comparable timeline: per-feed timestamp semantics and a documented synchronisation method with its error bound.
2. The applicable fee schedule for our account type, with effective dates.
3. Confirmation of the historical data product that corresponds to this instrument, including history depth, delivery format and price.
4. A sample file (one trading day is sufficient) so we can validate the schema and timestamp semantics before purchase.
5. Any entitlement, membership or minimum-commitment requirement.

Please state explicitly if any of these is not published or not available, and what the alternative would be. We would rather have an explicit 'not available' than an approximation, because our feasibility decision depends on it.

Thank you.
```

## WHERE TO RETURN THE ANSWER

Write the response into `M1/work/patches/UNK-0018-USSTOCK-CROSS_response.json` using the patch schema in
`M1/orchestrator/schemas.py`, or paste the raw vendor document into
`M1/work/external_requests/UNK-0018-USSTOCK-CROSS_received/` and record the patch by hand.
